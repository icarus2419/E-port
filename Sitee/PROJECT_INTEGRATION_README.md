# Project integration summary

Current featured portfolio projects:

1. `project/securedoc-combined/`
   - Case study: `project/securedoc-combined/index.html`
   - Static demo entry: `project/securedoc-combined/demo/`
   - Nested approval app demo: `project/securedoc-combined/demo/app/`
   - Original approval app source reference: `project/securedoc-combined/source-reference/`
   - The approval app demo uses a browser-local API shim so it runs on static hosting without a separate Express server.

2. Coming Soon project card
   - Placeholder card only.
   - No demo or case-study link until the next project is ready.

Shared project transitions live in:
- `assets/project-transitions.css`
- `assets/project-transitions.js`

Existing poker transition files were left in place and continue to drive the poker Play/Back flow.

The previous separate landing project and standalone approval project were removed from the homepage Projects section. SecureDoc is presented as the document approval portfolio entry.
